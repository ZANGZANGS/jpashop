spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
          # show_sql: true # system.out ?? ?? ????? false
          format_sql: true

logging:
  level:
    org.hibernate.SQL: debug #hibernate? ???? ???? ??
    org.hibernate.type: trace